<% breadcrumb :youreis, @word %>

<h1><% @word.id %>【<%= @word.content %>】</h1>

<!-- <p>
<%= link_to '尻取りを編集', edit_word_path(@word), class: "btn btn-primary" %>
<%= link_to '尻取りを削除', @word, method: :delete, data: { confirm: '用例も消えちゃうよ' }, class: "btn btn-danger" %>
</p> -->
<div class="row">
  <div class="col-xs-10">
    <%= form_for(@yourei) do |f| %>
    <div class="form-group">
      <%= f.label :content, '用例は？' %>
      <%= f.text_field :content, class: 'form-control' %>
      <%= hidden_field_tag :word_id, @word.id %>
    </div>
    <%= f.submit 'こう使う！', class: "btn btn-primary" %>
   <% end %>
  </div>
</div>

<table class="table table-striped table-responsive">

 <thead>
   <tr class="input-sm info">
      <th>用例</th>
      <th>作った人</th>
      <th>書き換える?</th>
   </tr>
 </thead>

 <tbody>
   <% @youreis.each do |yourei| %>
   <tr>
      <td><%= yourei.content %></td>
      <td>by <%= yourei.user.username %></td>
    <% if current_user == yourei.user %>
      <td><%= link_to yourei.id, yourei, class: "btn btn-primary" %></td>
    <% else %>
      <td>《ヨクナイネ！》ボタンをつけたい。</td>
    <% end %>
  </tr>
  <% end %>

  <!-- <tr><td><%= paginate @youreis %></td></tr> -->

  <% @wy.each do |yourei| %>
  <tr>
      <td><%= yourei.content %></td>
      <td>by <%= yourei.user.username %></td>
    <% if current_user == yourei.user %>
      <td><%= link_to yourei.id, yourei, class: "btn btn-primary" %></td>
    <% else %>
      <td>《ヨクナイネ！》ボタンをつけたい。</td>
    <% end %>
  </tr>
  <% end %>

  <!-- <tr><td><%= paginate @wy %></td></tr> -->

 </tbody>

</table>
