<h1>id: <%= @word.id %> の尻取り編集ページ</h1>

<%= form_for(@word) do |f| %>
  <%= f.label :content, '尻取り' %>
  <%= f.text_field :content %>

  <%= f.submit '尻取り！' %>
<% end %>

<%= link_to '尻取り一覧に戻る', words_path %>
